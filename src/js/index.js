import $ from 'jquery';
import topMenu from '../../src/topMenu/topMenu';
import dockerMenu from '../../src/dockerMenu/dockerMenu';
import '../../src/css/main.scss';

$(document).ready(function () {
    topMenu();
    //dockerMenu();
});